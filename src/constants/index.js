export const content = `## Hi there,

  <p>
    this is a very <em>basic</em> example of tiptap.
  </p>
  <pre><code>body { display: none; }</code></pre>
  <ul>
    <li>
      A regular list
    </li>
    <li>
      With regular items
    </li>
  </ul>
  <blockquote>
    It's amazing 👏
    <br />
    – mom
  </blockquote> `
export const json = 'Update content to see changes'
export const html = 'Update content to see changes'
